[tool:pytest]
markers =
    live: marks tests as live tests (deselect with '-m "not live"')

[coverage:run]
source = src/ai_file_organizer

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
